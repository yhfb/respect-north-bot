# دليل استضافة بوت ريسبكت الشمال 🚀

لضمان تشغيل البوت 24/7 مجاناً وبدون أخطاء، أنصحك باستخدام منصة **Koyeb**. اتبع الخطوات التالية بدقة:

### 1. تجهيز الكود
- تأكد من أن ملفك الأساسي هو `bot_fixed.py`.
- تأكد من وجود ملف `requirements.txt` الذي يحتوي على المكتبات المطلوبة.

### 2. الرفع إلى GitHub
- قم بإنشاء مستودع (Repository) جديد على حسابك في GitHub.
- ارفع الملفات (`bot_fixed.py`, `requirements.txt`, `Dockerfile`) إلى المستودع.

### 3. الإعداد على Koyeb
1. سجل في [Koyeb.com](https://www.koyeb.com/) (مجاني).
2. اضغط على **Create App**.
3. اختر **GitHub** كمصدر، وقم بربط حسابك واختيار المستودع الذي رفعته.
4. في قسم **Environment Variables** (مهم جداً)، أضف المفاتيح التالية:
   - `DISCORD_TOKEN`: (التوكن الخاص بك)
   - `GROQ_API_KEY`: (مفتاح Groq الخاص بك)
   - `OPENAI_API_KEY`: (مفتاح OpenAI إذا توفر)
5. في قسم **Build and Run settings**:
   - الـ Run command يجب أن يكون: `python bot_fixed.py`
6. اضغط على **Deploy**.

### 4. إبقاء البوت مستيقظاً (UptimeRobot)
بعد تشغيل البوت على Koyeb أو Render، ستحصل على رابط (URL) للموقع الخاص بالبوت.
1. اذهب إلى [UptimeRobot.com](https://uptimerobot.com/).
2. اضغط على **Add New Monitor**.
3. اختر نوع الـ Monitor: **HTTP(s)**.
4. ضع الرابط الذي حصلت عليه من الاستضافة.
5. اجعل وقت الفحص كل 5 دقائق.
بهذه الطريقة، سيظل البوت "مستيقظاً" ولن يتوقف أبداً!

### نصائح لتجنب الأخطاء:
- **لا تضع التوكنات داخل الكود:** استخدم دائماً متغيرات البيئة (Environment Variables) في الاستضافة.
- **استقرار الاتصال:** استضافة Koyeb توفر استقراراً ممتازاً للبوتات ولا تتطلب منك إبقاء جهازك مفتوحاً.
- **التحديثات:** أي تعديل ترفعه على GitHub سيقوم Koyeb بتحديث البوت تلقائياً.

---
**مبروك! بوتك الآن يعمل 24/7 في ريسبكت الشمال! 🔥**
